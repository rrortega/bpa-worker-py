version: '3.2'

services:
  bpa-worker-py:
    image: rrortega/bpa-worker-py:latest
    environment:
      PYTHONWARNINGS: "ignore:Unverified HTTPS request"
      PYTHONUNBUFFERED: 1
      JSON_B64: ${JSON_B64}
      ACTION: ${WOEKER_ACTION}
      RETHINKDB_HOST: '143.198.39.8'
      RETHINKDB_DATABASE: 'remesita'
      RETHINKDB_PASSWORD: 'L4cl4v3*RethinkDb'
    volumes:
      - ./crawler.py:/crawler.py
      - ./prpubsub.py:/prpubsub.py
      - ./main.py:/main.py
